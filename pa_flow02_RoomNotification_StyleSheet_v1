<style>
        body { 
            font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif; 
            margin: 0; 
            padding: 20px; 
            background-color: #f8f9fa; 
        }
        
        .event-card {
            max-width: 580px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0,0,0,0.08);
            border: 1px solid #e1e4e8;
        }

        /* Header & Banner */
        .card-header {
            background-color: #00426a;
            background-image: url('https://alaskaair.sharepoint.com/sites/CorporateRealEstate/SiteAssets/GTCEventsApp/GTC-exterior2.jpg');
            background-size: cover;
            background-position: center;
            min-height: 160px;
        }

        .header-content {
            padding: 35px 0 35px 25px;
        }

        .glass-overlay {
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            display: inline-block;
            padding: 15px 25px;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .sub-brand {
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
            color: #92c353;
            margin-bottom: 4px;
            letter-spacing: 1px;
        }

        .main-title {
            font-size: 24px;
            font-weight: 600;
            color: #ffffff;
            line-height: 1.2;
        }

        /* Body Content */
        .card-body {
            padding: 25px;
        }

        .event-summary {
            font-size: 18px;
            font-weight: 700;
            color: #333333;
            margin-bottom: 15px;
            border-bottom: 2px solid #f0f0f0;
            padding-bottom: 10px;
        }

        .label {
            font-size: 12px;
            font-weight: bold;
            text-transform: uppercase;
            color: #666666;
            margin-bottom: 5px;
        }

        .contact-list {
            font-size: 14px;
            color: #444444;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        /* Action Links Area */
        .content-footer {
            padding: 0 25px 25px 25px;
        }
        
        .link-button { 
            display: block; 
            text-decoration: none; 
            margin-top: 12px;
            background-color: #fcfcfc;
            border: 1.5px solid #ececec;
            border-radius: 8px;
            transition: all 0.2s ease;
        }
        
        .link-button:hover { 
            background-color: #f0f4f7; 
            border-color: #01426A; 
        }

        .link-table { width: 100%; border-collapse: collapse; }
        .icon-cell { width: 48px; padding: 12px 0 12px 16px; vertical-align: middle; }
        .text-cell { padding: 12px 16px; vertical-align: middle; font-weight: 600; font-size: 15px; color: #01426A; }
        
        .icon-img { width: 24px; height: 24px; display: block; }
    </style>
